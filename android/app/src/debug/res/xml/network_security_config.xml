<?xml version="1.0" encoding="utf-8"?>
<network-security-config>
    <!-- Debug build: allow all cleartext traffic for local dev (Vite, API, emulator) -->
    <base-config cleartextTrafficPermitted="true" />
</network-security-config>

